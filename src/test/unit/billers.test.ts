import { expect } from 'chai';
import BaxiPay from "../../../index"
require('ts-mocha');


describe('USER', () => {
    describe('GET /items', () => {
        it('Should return all items from an external api', () => {
            const baxipay = new BaxiPay("hello", "hello")
        });

    })

})
import { expect } from 'chai';
import BaxiPay from "../../../index"
require('ts-mocha');


describe('USER', () => {
    describe('GET /items', () => {
        it('Should return all items from an external api', () => {
            const baxipay = new BaxiPay("hello", "hello")
        });

    })

})
